class CoachWorkoutInAppAlert {
  const CoachWorkoutInAppAlert({
    required this.workoutId,
    required this.title,
    required this.distanceMeters,
  });

  final String workoutId;
  final String title;
  final double distanceMeters;
}
